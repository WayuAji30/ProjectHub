let slider = tns({
    container: ".my-slider",
    slideBy: 1,
    speed: 1000,
    nav: false,
    controlsContainer: "#controls",
    autoplay: true,
    autoplayTimeout: 10000,
    autoplayButtonOutput: false,
});
